// DiscordChat.jsx
// Feature: Discord-style chat system for students, teachers, admins

import React from "react";

// TODO: Add support for channels, DMs, mentions (@s, @t, @ad, etc.), and media.
const DiscordChat = () => {
  return (
    <div>
      <h2>Class Chat</h2>
      {/* Chatroom and message thread UI */}
    </div>
  );
};

export default DiscordChat;